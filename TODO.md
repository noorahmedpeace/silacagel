# DryGelWorld Growth TODO

Actionable checklist derived from the combined audit (Content/On-page SEO, GEO/AI-search, Structured Data, CRO/UX, Information Architecture, Content-Gap, and Trust/Lead-gen lenses). Items are grouped by category and ordered by priority within each group. File references are included where the fix is code-specific.

---

## Technical

- [ ] Fix the Product Offer price mismatch: `products/[slug]/page.tsx` lines 1010-1017 emit a concrete `Offer.price` (0.0035/0.0045/0.078/4.2 USD) that appears nowhere on the page (visible text is `product.priceBand` at line 709, e.g. "Tiered industrial reference rates available"). Either render "From $0.0035/unit" so the visible price byte-matches, or switch to `offers.priceSpecification` UnitPriceSpecification min/maxPrice and show that band on-page. This is the entire Product rich-result surface for the 4 money pages.
- [ ] Add a `Service` node (provider @id pattern from `export/[market]/page.tsx:169-201`) for quote-only product slugs (calcium-chloride-container-strip/bulk, both nitrile gloves, hair-nets, beard-covers, dry-clay-desiccant) in `products/[slug]/page.tsx` — currently gated on `offerPricing` (line 998), so ~8 slugs carry no offering entity.
- [ ] Add `CollectionPage` + `ItemList` + `BreadcrumbList` JSON-LD to `products/page.tsx` (flagship hub currently emits none); mirror the pattern in `compare/page.tsx:132-155` and `industries/page.tsx:92-108`. Do the same for `/blog` and `/export` index pages.
- [ ] Replace inlined publisher/parentOrganization objects with `{ "@id": "${absoluteUrl()}#organization" }` in `blog/[slug]/page.tsx:251-259`, `compare/[slug]/page.tsx:229-237`, `guides/silica-gel-buyer-guide` lines 753-761, and `authors/[slug]/page.tsx:118-122` — match the correct `case-studies/[slug]/page.tsx:195-200` pattern to stop creating duplicate unlinked Organization entities.
- [ ] Make the RFQ fallback resilient in `quote-form.tsx` (lines 180, 189): the bare `window.location.href = mailto` silently fails on desktops with no mail client while the UI claims "We opened your email client" (lines 462-472). POST to a capture endpoint or show a copyable WhatsApp/email option instead.
- [ ] Compute `Offer.priceValidUntil` dynamically (today + 1 year) instead of the hardcoded `"2026-12-31"` in `products/[slug]/page.tsx:1014` — it silently expires Product eligibility at year-end.
- [ ] Remove the duplicate `/private-label` sitemap emission (STATIC_ROUTES line 46 and the seoLandingPages loop line 109); audit `/drygelworld` for the same duplicate-URL issue.
- [ ] Add `priceRange` (e.g. "$$") to the LocalBusiness node in `layout.tsx:457-487` to clear the validator recommendation, or reconsider whether LocalBusiness adds value for a non-walk-in exporter.
- [ ] Delete the orphaned mobile hero CSS block in `page.module.css:3062-3170` (targets `.hero`, `.heroBgImage`, `.heroTrustBar`, `.whatsAppFloat` etc. no longer in the `heroX*` markup); audit `site-header.module.css` for overlapping dead `.nav`/`.menuToggle` rules.
- [ ] Reset internal expectations on deprecated rich results: HowTo (removed 2023) and FAQPage (restricted to gov/health) will not render SERP enhancements for a commercial domain (`page.tsx:590-615`, `container-desiccant-calculator` page.tsx:134-147, `faq-block.tsx`, `seo-landing-pages.ts:4557-4566`). Keep markup for GEO value; do not invest further here.

## SEO

- [ ] Fix the 15 zero-inbound-link orphan landing pages (sitemap-only, no PageRank): silica-gel-supplier-karachi (HQ city), silica-gel-manufacturer-china-alternative, hair-net-supplier-uae/-usa/-saudi-arabia, beard-cover-supplier-usa/-uk/-saudi-arabia, dry-clay-desiccant-supplier-uae/-saudi-arabia, 21-inch-hair-nets, non-woven-beard-covers, desiccants-for-pharma-industry, hair-nets-for-food-industry, moisture-absorber-for-shipping. Add contextual links from footer, matching `/export/[market]` relatedLinks, `/products/*`, and `/industries/*`.
- [ ] Consolidate the container / shipping-container desiccant cluster (6 pages: `seo-landing-pages.ts` slugs at :926, :995, :3369, :4222, :1627, :2805). Designate `/shipping-container-desiccant-supplier` as the commercial hub, keep `/container-desiccant-strips` as format page, 301 the other three into the hub with sections, cross-link the 3 blog guides.
- [ ] Merge the moisture-absorber near-duplicates: keep `/moisture-absorber-supplier` (`seo-landing-pages.ts:4137-4186`), 301 `/moisture-absorber-manufacturer` (:1588-1624) into it, fold unique copy into a section.
- [ ] Merge the bare-term vs -supplier twins: container-desiccant (:996) vs container-desiccant-supplier (:927); industrial-desiccant (:771) vs industrial-desiccant-supplier (:732); desiccant-bags (:1117) vs desiccant-bags-supplier (:1078); plus the silica-gel-packets trio (packets/-manufacturer/-wholesale). Pick the head-term winner, 301 the loser, update relatedLinks.
- [ ] Build an HTML hub for the 64-page landing tier (e.g. `/desiccant-solutions`) grouped by theme (grades, formats, industry/use, geo/supplier, PPE); mirror `compare/page.tsx`/`industries/page.tsx` dynamic mapping and add it to the header dropdown so every landing page gets a durable inbound link.
- [ ] Add homepage internal links from `page.tsx`: make `IndustryScrolly` cards link to their `/industries/[slug]` (currently only `/contact` via `industry-scrolly.tsx`); add an "Explore by format" block to 4-6 `/products/[slug]`, the `/export` `/compare` `/industries` hubs, and 2-3 head-term landing pages.
- [ ] De-emphasize the PPE topical dilution: 18 of 66 landing pages target hair nets/beard covers (`seo-landing-pages.ts` slugs :1749–:2761). Either move PPE to a separate subdomain or collapse to 2-3 pages (hair-net-supplier, beard-cover-supplier, food-grade) and drop size/geo doorway variants from the sitemap.
- [ ] Consolidate the private-label/OEM cluster: nearly identical titles at `seo-landing-pages.ts:1284` and `:3544`, plus :1165 and :1206. Keep `/private-label-desiccant-packets` canonical, 301 the other three.
- [ ] Consolidate the generic silica-gel head: keep silica-gel-manufacturer (:449) and silica-gel-supplier (:572) as distinct pillars, fold desiccant-manufacturer (:693) into manufacturer, keep china-alternative (:1037) only if it has unique competitor copy; ensure each pillar has differentiated H1/body, not template boilerplate.
- [ ] Resolve geo-vs-export cannibalization: 301 standalone `/silica-gel-supplier-uk` (:2014), `/silica-gel-manufacturer-pakistan` (:3645), `/silica-gel-supplier-karachi` (:3730) into their richer `/export/*` equivalents (`markets.ts`), keeping only genuinely distinct product-geo pages.
- [ ] Rename `/bentonite-clay` slug to `/clay-desiccant-supplier` (301 old, keep "bentonite clay" as body synonym) in `seo-landing-pages.ts:2320-2329` and make it the hub the three geo clay pages and `/products/dry-clay-desiccant` link into.
- [ ] Create the missing `/export-packaging-desiccant` (or `/moisture-barrier-export-packaging`) commercial hub — "export packaging" is a target term with no landing page (only blog posts `articles.ts:979`, :2054).
- [ ] Add visible breadcrumb navigation: a shared Breadcrumb component driven by the arrays already passed to `breadcrumbJsonLd`, rendered at top of product, blog, landing, compare, industry, case-study pages; make `product.categoryPath` segments real `<Link>`s (`products/[slug]/page.tsx:632-634`).
- [ ] De-duplicate templated doorway pages: hair-net size variants (`seo-landing-pages.ts:2145-2274`) share identical image/buyerTypes/documents differing only by size number; the `keywordClusterPage()` boilerplate (specsIntro/buyingIntro/buyingSteps at :160-191) is byte-identical across ~40 URLs. Replace size variants with anchor sections on one parent and vary per-page copy.
- [ ] Add calcium-chloride comparison links to the calcium-chloride product `related` arrays (`products/[slug]/page.tsx:110-131`) and cross-link the 7 CC comparison pages to each other — they are currently reachable only from the buried `/compare` hub.
- [ ] Deepen export hub-spoke linking: add an "Other export markets" block to `export/[market]/page.tsx` linking 4-6 siblings (11 markets incl. home-market `/export/pakistan` are hub-only), rotate tier-2 markets (Vietnam, Indonesia, Malaysia, Turkey, Mexico, Brazil, Bangladesh) into the footer.
- [ ] Add `/compare/silica-gel-vs-activated-alumina` (only a blog post exists, `compare-data.ts` has the CC variant at :560 but not this) and 1-2 "alternative to [Sorbead/Clariant/Container Dri]" comparison pages; link from the china-alternative page.
- [ ] Add explicit `blog-clusters.ts` entries for the 4 fallback articles (top-10-silica-gel-suppliers-world-pakistan, silica-gel-colors-white-blue-orange-explained, silica-gel-shelf-life-and-storage-guide, silica-gel-for-rice-grain-spice-export) so they get topically precise related links instead of the generic default (`blog-clusters.ts:356-364`).
- [ ] Add inbound links to the under-linked head-term pages: `/silica-gel` (1 footer link only) and `/buy-silica-gel` (2 links) from products/compare/industries hub intros and relatedLinks.
- [ ] Vary the generic secondary CTA anchor "View Product Range" → `/products` injected on every cluster page (`seo-landing-pages.ts:148`) to page-relevant anchors, and replace generic relatedLinks labels like "Hair net supplier (general)" (:2180) with target-keyword anchors.

## GEO (AI-search answerability)

- [ ] Author 3-4 buyer FAQs per remaining export market in `markets.ts` (MOQ, lead time to port, Incoterms/duty, docs). Only 5 of 20 markets have `faqs`, so 15 country pages (germany, uk, vietnam, india, brazil, etc.) emit no FAQPage — the render + JSON-LD (`export/[market]/page.tsx:204-215`) light up automatically once data exists.
- [ ] Populate the `customs` (HS code 2811.22/3824, dutyNote, tariffLookup, requiredDocs) and `moq`/`leadTime`/`incoterms` blocks for the remaining ~14 markets in `markets.ts` (currently 6 and 5 markets); interpolate `market.country` into the two hardcoded "Mexico" headings (`export/[market]/page.tsx:384`, :405) before extending.
- [ ] Emit a price-less `Product` node (brand + manufacturer + material, no `offers`) for the 6 quote-only catalog products (PPE + dry clay) in `products/[slug]/page.tsx` so they resolve as structured entities for AI extraction.
- [ ] Add 2-3 authoritative `sources` (DIN 55473, ISO 9001, REACH/ECHA, HS 2811.22, ASTM, glossary DefinedTerm anchors) to the 37 blog articles lacking them in `articles.ts` (only 9 of 46 have `sources`); References render + `Article.citation` wiring already exist (`blog/[slug]/page.tsx:262`).
- [ ] Surface the existing `authors.ts` `credentials`/`role` into the author JSON-LD (or add a named Person author with jobTitle + sameAs LinkedIn) on both blog (`blog/[slug]/page.tsx:244`) and compare (`compare/[slug]/page.tsx:217-223`) pages — currently an anonymous "Export Desk" org byline, the weakest E-E-A-T signal.
- [ ] Add an `ItemList`/`CollectionPage` on the homepage (`page.tsx`) referencing each `/products/<slug>#product` @id — the homepage currently emits only a single HowTo node and hands AI just 3 generic Service categories.
- [ ] Add `about`/`mentions` arrays to compare and blog `Article` nodes referencing relevant `/products/<slug>#product` @ids, the `#organization` @id, and glossary DefinedTerm @ids to connect the editorial → product → term entity graph.

## CRO / UX

- [ ] Mount `<WhatsAppFloat />` globally (built but never rendered — `whatsapp-float.tsx` is referenced only in its own file; `deferred-chrome.tsx:66-71` mounts only GlobalDiscountCampaign/ClarityBridge/DryBot). Add it inside DeferredChrome and confirm no overlap with the DryBot launcher. Highest-leverage mobile fix; `whatsapp_click` tracking already exists (`layout.tsx:245-249`).
- [ ] Stop hiding trust cues on mobile: `page.module.css:6425-6427` hides the ISO badge (`.heroXProof a`) and :6443-6445 hides all three hero trust signals (`.heroXSignals`). Show a compact ISO 9001 + "Since 1983 / 190+ markets" trust chip on mobile instead.
- [ ] Render `<StickyQuoteBar href="#contact" />` on the homepage (`page.tsx`, RFQ section id is "contact" at :574) — it is currently only on landing/product pages, and its default `#quote-form` href does not exist on the homepage so pass `#contact` explicitly.
- [ ] Add a fallback action to the homepage price calculator (`price-calculator.tsx`): it dead-ends into WhatsApp only (`handleWhatsAppQuote` :136-172). Prefill the on-page RFQ / route to `/contact` with the estimate (QuoteForm accepts defaultProduct/defaultQuantity/defaultMessage) and relabel the button to disclose "Get quote on WhatsApp".
- [ ] Replace the generic anonymous testimonials (`page.tsx:195-220`: "Footwear exporter", "Electronics logistics team") with 2-3 permission-cleared named references using the already-built `attribution: { name, title, company, quote }` field in `case-study-data.ts`; add one verifiable external proof channel. Do NOT add aggregateRating without real reviews.
- [ ] Model genuine, attributable case-study/testimonial quotes as `Review` on Organization/Product (start with named case-study attributions, `case-studies/[slug]/page.tsx:120-131`) so they can roll up into aggregateRating — the highest-CTR SERP enhancement. Only real, permissioned reviews.
- [ ] Consolidate the split documentation path: `/documents` (request page) vs `/documentation` (actual downloadable ISO cert). Repoint the hero "Documents" signal (`page.tsx:86`) to `/documentation` where the PDF lives, and 301 or repurpose the duplicate.
- [ ] Replace the consumer-style FLASH10 discount countdown injected into the B2B RFQ/calculator (`price-calculator.tsx:107-109`, :261-294; `quote-form.tsx:147-153`) with a volume-tier / trade-terms incentive (e.g. "ask about tier pricing above X MOQ").
- [ ] Add DryBot failure fallback (`drybot.tsx` sendRfq :107-122): on submit failure auto-offer the prefilled WhatsApp/mailto link (reuse waHref/emHref), surface a persistent "Get a quote" chip, and consider requiring company + product for lead quality.
- [ ] Surface indicative USD price bands and a headline MOQ in the homepage pricing section (`page.tsx:156-160` currently all "quoted by requirement") or link the chips to the existing `/pricing` page; consider pointing the hero CTA closer to the RFQ than the mid-funnel `/buy-silica-gel`.
- [ ] Promote the "honest scope" compliance differentiator (which certs are/aren't held, `about/page.tsx honestScope`, `certifications/page.tsx`) into a condensed homepage trust block and export landing pages, linking to the full matrix.
- [ ] Add a concrete, defensible guarantee (e.g. free replacement/credit for any batch failing its published COA spec; sample dispatched within X days) to `guarantee-strip.tsx` (currently makes no promise by design) — keep it real, consistent with the honesty discipline.
- [ ] Anchor the self-asserted scale figures (10M+ packets, 10,000+ customers, `trust-band.tsx:21-24`) to verifiable proof: cross-link the sister brand SilicaGelPK and, if available, publish a business/export registration doc in `document-registry.ts` beside the ISO cert.

## Content

- [ ] Create a troubleshooting/problem-solution cluster (`/guides/troubleshooting` or blog) — zero exists among 43 articles: "Mould in a shipping container even with desiccant", "Condensation inside sealed packaging: diagnosis and fix", "Cargo arrived wet — desiccant post-mortem checklist", "Why silica gel packets are still wet on arrival". Each ends with a sizing/selector CTA. High-intent, low-competition, AI-answerable.
- [ ] Add exact-match "best desiccant for [electronics/pharma/food-packaging/metal-parts]" verdict pages under `/guides` — only `best-desiccant-for-shipping-containers` exists (`articles.ts:1583`). Lead with the material recommendation + mini decision table (from `compare-data.ts`) and link to matching industry page + product + selector tool; differentiate from the supplier-intent industry pages.
- [ ] Add 3-5 substantive export country pages first (Nigeria, Egypt, South Africa, Netherlands EU hub, Oman/Kuwait) in `markets.ts` — Africa is entirely absent. Populate the optional commercial-terms block (`markets.ts:13`) so they are not thin doorway pages; skip a blanket rollout.
- [ ] Add `/guides/silica-gel-hs-code-import-duty-by-country` as a structured, filterable table (destination, HS code, duty band, required docs), cross-linked from the HS-code blog (`articles.ts:3663`) and each `/export/[market]` page — a durable link/citation asset.
- [ ] Add a `/guides` (or `/resources`) pillar hub linking all calculators, comparison pages, industry guides, and the glossary grouped by buyer job (aggregation/internal-linking only, no new content).
- [ ] Extend the `BlogArticle` section type to allow inline contextual links (reuse the mixed text/link `parts` pattern from `seo-landing-page.tsx:123-140`) and seed 3-5 descriptive in-body anchors per article to product/export/compare pages — 43 articles currently pass equity only through the footer cluster widget.
- [ ] Do NOT add new material-vs-material comparison pages — silica vs clay/molecular-sieve/oxygen-absorber/calcium-chloride/bentonite/VCI/activated-carbon all already exist in `compare-data.ts`. Invest the comparison budget in tools and troubleshooting instead.

## Features (Tools / Lead-gen)

- [ ] Build `src/app/tools/desiccant-unit-calculator` (DIN 55473 / MIL-D-3464): inputs = package volume or barrier area, MVTR class, transit days; output = required units + nearest DryGelWorld pack format + "Get this quoted" CTA. Reuse `moisture-load-calculator/page.tsx` scaffold; cross-link the two existing DIN/packets blog articles (`articles.ts:3977`, :4081). The most-searched procurement calculation in this niche.
- [ ] Build `src/app/tools/desiccant-selector` guided wizard (cargo type → route/enclosure → volume → recommended product slug + prefilled QuoteForm). Drive branching from existing `compare-data.ts` `decisions` arrays and `product-data.ts` fit fields (single source of truth).
- [ ] Build `src/app/tools/moisture-damage-roi-calculator`: inputs = cargo value, claim frequency, containers/month, transit humidity; output = annualised risk vs protection cost + prefilled RFQ CTA. Reuse `container-dosage-model.ts` logic. The USD business case is the highest-converting B2B lead magnet and the calculators currently only output grams.
- [ ] Create a dedicated `/samples` (or `/free-sample`) landing with a short 3-field form (email, product, destination) — sample intent is currently buried in one `sampleNeed` dropdown inside the collapsed RFQ section (`quote-form.tsx:416-426`). Link from hero, product pages, and DryBot.
- [ ] Build a `/tools` compliance-finder: user picks destination + application → exact required document set (SDS/COA/DMF-free/ISO) with honest "held vs confirm-at-RFQ" answer → prefilled RFQ. Combines `markets.ts` per-country data + `about` honestScope (reuses existing data, no new claims).
- [ ] Add `src/app/tools/page.tsx` hub listing all calculators/tools with descriptions and RFQ CTAs (no `/tools` index currently exists); link from header Resources group and footer to consolidate crawl/link equity and rank for "silica gel calculator/tools" queries.

## Off-site / Backlinks

- [ ] Secure written permission from 2-3 repeat buyers/distributors to display logo + one-line named reference; add a "trusted by" logo-band component above the footer near `GuaranteeStrip` and wire into the `case-study-data.ts attribution` schema (all 10 case studies are currently anonymized).
- [ ] Establish and link one verifiable external review channel (Google Business Profile, verified WhatsApp Business, or Alibaba/trade-platform storefront badge) from the homepage — the strongest borrowed-credibility signal for a new low-authority exporter domain.
- [ ] Add the sister-brand SilicaGelPK cross-link next to the scale stats to anchor self-asserted figures to an existing storefront (`trust-band.tsx`).
- [ ] Treat the new interactive tools (unit calculator, ROI calculator, HS-code duty table) as backlink/citation magnets — internal links are the only PageRank source until backlinks arrive, so promote these assets for outreach and AI-engine citation once live.
